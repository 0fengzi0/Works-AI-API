# Works-AI-API 项目需求文档

## 1. 项目概述

Works-AI-API 是一个面向多渠道大模型调用的 API 聚合平台。平台采用前后端分离架构，前端使用 Vue，后端运行在 Cloudflare Worker，数据库使用 D1。

平台核心目标是为用户提供统一的大模型接入、Token 计费、秘钥管理、充值核销和使用统计能力，并以 OpenAI 兼容接口作为主要对外协议，方便现有客户端快速接入。

## 2. 建设目标

- 统一聚合多个大模型渠道，降低接入复杂度。
- 为用户提供标准化登录、注册、充值、调用、查询和统计能力。
- 为管理员提供模型、秘钥、卡密、用户和计费的统一后台管理能力。
- 支持基于 Token 的精细化计费与消费追踪。
- 支持多 Key 轮询，提高渠道稳定性与可用性。

## 3. 技术栈

- 前端：Vue3 + TypeScript + Vite + Naive UI
- 后端：Cloudflare Worker
- 数据库：Cloudflare D1
- 认证：JWT
- 接口协议：OpenAI 兼容格式
- 流式响应：SSE

## 4. 角色定义

### 4.1 普通用户

- 注册、登录、退出登录
- 创建和管理自己的平台调用秘钥
- 查看可用模型列表和价格
- 发起大模型 API 调用
- 查看 Token 余额、消费记录和调用日志
- 使用卡密充值 Token

### 4.2 管理员

- 管理用户账号
- 管理模型与定价
- 管理渠道秘钥
- 管理卡密生成与核销
- 查看全局日志、统计和消费概览
- 配置限流、轮询和其他平台参数

## 5. 功能需求

### 5.1 用户注册与登录

#### 5.1.1 注册

- 支持邮箱注册。
- 注册时只需要邮箱和密码，不强制邮箱验证。
- 密码需要满足基础复杂度要求。
- 注册成功后自动创建用户账户和默认资源记录。

#### 5.1.2 登录

- 支持邮箱 + 密码登录。
- 登录成功后签发 JWT。
- JWT 用于后续接口鉴权。

#### 5.1.3 账号状态

- 支持正常、禁用、冻结等状态。
- 禁用或冻结后无法继续调用 API。

### 5.2 大模型聚合

#### 5.2.1 对外接口格式

- 平台对外提供 OpenAI 兼容接口。
- 优先支持 `chat/completions` 类接口。
- 后续可扩展 embeddings、images、audio 等能力。

#### 5.2.2 支持范围

- 支持主流国内外大模型渠道。
- 支持不同厂商模型统一接入和管理。
- 模型映射关系由管理员后台维护。

#### 5.2.3 流式响应

- 支持 SSE 流式响应。
- 流式返回格式尽量兼容 OpenAI 客户端预期。

#### 5.2.4 模型管理

- 管理员可新增、编辑、启用、禁用模型。
- 每个模型可配置所属渠道、真实模型名、显示名称、上下文长度、输入单价、输出单价、状态等信息。
- 支持按模型分别定价。

### 5.3 秘钥管理

平台秘钥分为两类：用户平台调用秘钥和管理员渠道秘钥。

用户平台调用秘钥用于用户调用 Works-AI-API 对外接口；管理员渠道秘钥用于平台转发请求到第三方大模型渠道。两类秘钥用途不同，不能混用。

#### 5.3.1 用户平台调用秘钥

- 用户可在控制台创建自己的平台调用秘钥。
- 平台调用秘钥用于 OpenAI 兼容 API 的鉴权。
- 用户可创建多个调用秘钥，便于在不同项目或客户端中隔离使用。
- 用户可查看调用秘钥名称、创建时间、最后使用时间、状态等信息。
- 用户可禁用、启用、删除自己的调用秘钥。
- 调用秘钥只在创建时完整展示一次，后续只展示脱敏信息。
- API 请求使用平台调用秘钥时，需要关联到对应用户并执行余额、限流、模型权限和账号状态校验。

#### 5.3.2 管理员渠道秘钥

- 管理员渠道秘钥指各渠道大模型 API Key。
- 管理员统一维护渠道 Key，用户不直接接触渠道 Key。
- 同一渠道支持配置多个 Key。
- 系统支持 Key 轮询和负载均衡。

#### 5.3.3 Key 状态

- 用户平台调用秘钥支持启用、停用、删除等状态。
- 管理员渠道秘钥支持启用、停用、失效、限额等状态。
- 支持记录每个 Key 的使用次数、失败次数和可用性状态。

### 5.4 Token 计费

#### 5.4.1 计费规则

- 计费模型按不同模型分别定价。
- 计费至少区分输入 Token 和输出 Token。
- 不同模型可配置不同单价。

#### 5.4.2 扣费方式

- 用户发起请求时，根据最终实际消耗 Token 扣费。
- 流式和非流式请求都需要支持计费。
- 请求失败时，是否扣费按错误类型判断，建议只对实际完成的有效调用扣费。

#### 5.4.3 余额管理

- 用户账户维护 Token 余额。
- Token 余额不足时禁止继续调用。
- 充值后余额即时可用。

### 5.5 卡密系统

#### 5.5.1 卡密生成

- 管理员可在后台生成卡密。
- 卡密面额支持自定义。
- 每张卡密对应固定 Token 数量。
- 支持批量生成卡密。

#### 5.5.2 卡密核销

- 用户输入卡密后进行核销。
- 核销成功后，Token 自动充值到用户账户。
- 卡密只能核销一次。
- 已核销、过期、失效卡密不可重复使用。

#### 5.5.3 卡密管理

- 支持查看卡密状态、面额、创建时间、核销时间、核销用户等信息。
- 支持导出卡密数据。

### 5.6 调用日志与统计

- 记录每次 API 调用日志。
- 记录请求时间、用户、模型、渠道、输入 Token、输出 Token、总消耗、耗时、状态、错误信息等。
- 提供按用户、模型、时间范围的统计能力。
- 提供消费明细和调用趋势图表。

### 5.7 限流控制

- 平台需要支持 API 调用频率限制。
- 可按用户等级、账户状态或全局策略控制 RPM/TPM。
- 超出限制时返回明确错误信息。

### 5.8 管理后台

- 用户管理：查询、禁用、解禁、查看余额和调用情况。
- 模型管理：配置模型、价格、状态、映射关系。
- 秘钥管理：配置渠道 Key、轮询策略和状态，查看用户平台调用秘钥使用情况。
- 卡密管理：生成、查询、导出、作废。
- 日志统计：查看调用量、消耗量、成功率和错误分布。
- 系统设置：限流、基础参数、公告等。

## 6. 核心业务流程

### 6.1 用户注册登录流程

1. 用户填写邮箱和密码注册。
2. 系统创建用户账户。
3. 用户使用邮箱和密码登录。
4. 系统签发 JWT。
5. 用户使用 JWT 访问平台接口。

### 6.2 模型调用流程

1. 用户创建平台调用秘钥。
2. 用户使用平台调用秘钥选择可用模型并发起请求。
3. 系统校验调用秘钥、余额和限流规则。
4. 系统根据模型映射找到可用渠道 Key。
5. 请求转发给对应渠道。
6. 系统接收响应并返回给用户。
7. 系统记录日志并扣减 Token。

### 6.3 卡密充值流程

1. 管理员生成卡密并分配面额。
2. 用户输入卡密进行核销。
3. 系统校验卡密状态。
4. 核销成功后 Token 自动入账。
5. 系统记录充值日志。

## 7. 数据实体建议

- 用户 User
- 管理员 Admin
- 用户平台调用秘钥 UserApiKey
- 模型 Model
- 渠道 Key ChannelKey
- 卡密 Voucher
- 充值记录 RechargeRecord
- 调用日志 ApiLog
- 计费记录 BillingRecord
- 限流策略 RateLimitPolicy
- 系统配置 SystemSetting

## 8. 非功能需求

- 安全性：密码加密存储，JWT 签名校验，敏感配置不得明文暴露。
- 可用性：支持多 Key 轮询，降低单 Key 故障影响。
- 可扩展性：模型、渠道、价格、限流策略均应可后台配置。
- 审计性：关键动作需要留痕，包括登录、核销、扣费和后台修改。
- 性能：高频调用场景下仍需保持低延迟。

## 9. 页面建议

### 9.1 前台页面

- 首页
- 登录 / 注册
- 控制台
- 调用秘钥管理
- 模型列表
- API 文档
- Token 余额与充值
- 调用日志
- 消费统计

### 9.2 后台页面

- 仪表盘
- 用户管理
- 模型管理
- 渠道 Key 管理
- 卡密管理
- 日志中心
- 系统设置

## 10. 里程碑建议

### 第一阶段

- 完成用户注册登录
- 完成 JWT 鉴权
- 完成基础模型调用链路
- 完成 D1 数据表设计

### 第二阶段

- 完成 Token 计费
- 完成卡密生成与核销
- 完成日志与统计

### 第三阶段

- 完成后台管理
- 完成多 Key 轮询和限流
- 完成文档与接口整理

## 11. 待确认事项

当前阶段已明确不包含以下能力：

- 不支持邮箱验证码注册或找回密码。
- 暂不支持除 `chat/completions` 之外的其他 OpenAI 兼容接口。
- 不支持团队、子账号或邀请返利。
- Token 余额不设置过期机制。
- 不提供公开 API 文档站点。
